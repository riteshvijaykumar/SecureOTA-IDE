{
  "name": "electron-app",
  "version": "0.1.0",
  "private": true,
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "lint": "echo \"lint not configured yet\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^30.1.2"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
